# V1 - 90% of traffic
apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-tracker-v1
  namespace: task-tracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task-tracker
      version: v1
  template:
    metadata:
      labels:
        app: task-tracker
        version: v1
    spec:
      containers:
      - name: task-tracker
        image: csmohmmadsh/task-tracker:v6
        ports:
        - containerPort: 8000
        env:
        - name: APP_VERSION
          value: "v1"
        - name: DB_HOST
          value: postgres
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
# V2 - 10% of traffic (CANARY)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-tracker-v2
  namespace: task-tracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task-tracker
      version: v2
  template:
    metadata:
      labels:
        app: task-tracker
        version: v2
    spec:
      containers:
      - name: task-tracker
        image: csmohmmadsh/task-tracker:v6
        ports:
        - containerPort: 8000
        env:
        - name: APP_VERSION
          value: "v2"
        - name: DB_HOST
          value: postgres
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: task-tracker-v1
  namespace: task-tracker
spec:
  selector:
    app: task-tracker
    version: v1
  ports:
  - port: 80
    targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: task-tracker-v2
  namespace: task-tracker
spec:
  selector:
    app: task-tracker
    version: v2
  ports:
  - port: 80
    targetPort: 8000